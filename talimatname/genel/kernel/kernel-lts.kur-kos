#!/bin/bash
surum=4.10.9
sonek=milis-lts
cd /boot
if [ -f kernel ];then
	rm kernel
fi
ln -s kernel-$surum kernel
if [ -f initramfs ];then
	mv initramfs initramfs_eski
fi
dracut -N --force --xz --omit systemd  /boot/initramfs $surum-$sonek
grub-mkconfig -o /boot/grub/grub.cfg
