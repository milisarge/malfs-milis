# Description: Hunspell, LibreOffice, OpenOffice.org, Mozilla Firefox 3 ve Thunderbird, Google Chrome'un yazım denetleyicisidir ve ayrıca macOS, InDesign, memoQ, Opera ve SDL Trados gibi tescilli yazılım paketleri tarafından da kullanılır.
# URL: http://hunspell.sourceforge.net/
# Packager: milisarge
# Depends on:

name=hyphen
version=2.8.8
release=1
source=( http://downloads.sourceforge.net/hunspell/$name-$version.tar.gz)

build() {
	cd $name-$version
        ./configure --prefix=/usr \
		    --disable-static
        make
        make DESTDIR=$PKG install
}
