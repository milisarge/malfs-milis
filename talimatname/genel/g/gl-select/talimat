# Description: gl/glx kütüphane seçicisi
# URL: talimatname/genel/gl-select
# Packager: milisarge
# Depends on: xorg-server

name=gl-select
version=1.6
release=1
source=(gl-select-kod)

build() {
  install -D -m 0755 -o root -g root $SRC/gl-select-kod $PKG/usr/bin/$name
}
