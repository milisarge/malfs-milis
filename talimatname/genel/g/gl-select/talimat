# Tanım: gl/glx kütüphane seçicisi
# URL: talimatname/genel/gl-select
# Paketçi: milisarge
# Gerekler: xorg-server

isim=gl-select
surum=1.6
devir=1
kaynak=(gl-select-kod)

derle() {
  install -D -m 0755 -o root -g root $SRC/gl-select-kod $PKG/usr/bin/$isim
}
